package com.the_attic_level.dash.ui.layout.type

enum class UIOrientation(val degrees: Double)
{
    // ----------------------------------------
    // Enum
    
    RIGHT (degrees =   0.0),
    DOWN  (degrees =  90.0),
    LEFT  (degrees = 180.0),
    UP    (degrees = 270.0)
}