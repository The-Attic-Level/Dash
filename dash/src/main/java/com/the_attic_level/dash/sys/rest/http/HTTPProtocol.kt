package com.the_attic_level.dash.sys.rest.http

enum class HTTPProtocol(val prefix: String)
{
    // ----------------------------------------
    // Cases
    
    HTTP  ("http://"),
    HTTPS ("https://")
}